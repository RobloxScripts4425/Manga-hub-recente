-- Carregar biblioteca Fluent
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

-- Criar a interface principal
local Window = Fluent:CreateWindow({
    Name = "Manga Hub 4.2.1",
    Footer = "Fluent UI",
    Color = Color3.fromRGB(44, 120, 224),
    Position = UDim2.new(0.5, -200, 0.5, -250),
    Size = UDim2.new(0, 400, 0, 500),
    CanDrag = true
})

-- Criar abas
local PlayerTab = Window:CreateTab({
    Name = "Jogador",
    Icon = "rbxassetid://4483345998"
})

local TrollTab = Window:CreateTab({
    Name = "Troll",
    Icon = "rbxassetid://4483345998"
})

-- Função de exemplo para ajustar a velocidade do jogador
PlayerTab:CreateButton({
    Name = "+30 Velocidade",
    Callback = function()
        local player = game.Players.LocalPlayer
        local character = player.Character
        if character and character:FindFirstChild("Humanoid") then
            character.Humanoid.WalkSpeed = character.Humanoid.WalkSpeed + 30
        end
    end
})

-- Função para restaurar velocidade e pulo
PlayerTab:CreateButton({
    Name = "Pulo e Velocidade Normal",
    Callback = function()
        local player = game.Players.LocalPlayer
        local character = player.Character
        if character and character:FindFirstChild("Humanoid") then
            character.Humanoid.JumpPower = 50 -- JumpPower padrão
            character.Humanoid.WalkSpeed = 16 -- WalkSpeed padrão
        end
    end
})

-- Função de exemplo para iniciar o Fling (básico)
TrollTab:CreateButton({
    Name = "Iniciar Fling",
    Callback = function()
        -- Função simples de Fling
        local player = game.Players.LocalPlayer
        local character = player.Character
        if character and character:FindFirstChild("HumanoidRootPart") then
            while true do
                wait(0.1)
                character.HumanoidRootPart.Velocity = Vector3.new(900, 0, 0)
            end
        end
    end
})

-- Parar o Fling e zerar a saúde
TrollTab:CreateButton({
    Name = "Parar Fling",
    Callback = function()
        local player = game.Players.LocalPlayer
        local character = player.Character
        if character and character:FindFirstChild("Humanoid") then
            character.Humanoid.Health = 0
        end
    end
})

-- Adicionar toggle para flutuar
PlayerTab:CreateToggle({
    Name = "Flutuar",
    Default = false,
    Callback = function(state)
        local player = game.Players.LocalPlayer
        local character = player.Character
        if character and character:FindFirstChild("HumanoidRootPart") then
            character.HumanoidRootPart.Anchored = state
        end
    end
})
